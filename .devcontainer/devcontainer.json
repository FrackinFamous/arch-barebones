{
    "name": "Arch Dev Container",
    "build": { "dockerfile": "Dockerfile" },
    "postCreateCommand": "sudo pacman -Syu --noconfirm && sudo pacman -S --noconfirm base-devel git",
    "remoteUser": "frack"
}
