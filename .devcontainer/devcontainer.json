{
    "name": "Arch Dev Container",
    "build": { "dockerfile": "Dockerfile" },
    "postCreateCommand": "sudo pacman -Syu --noconfirm && sudo pacman -S --noconfirm git openssh",
    "remoteUser": "frack"
}
