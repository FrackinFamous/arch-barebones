{
    "name": "Arch Dev Container",
    "build": { "dockerfile": "Dockerfile" },
    "postCreateCommand": "ls -l ~/.devcontainer && whoami && bash ~/.devcontainer/post-create.sh",
    "remoteUser": "frack"
}
